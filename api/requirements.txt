# kraken + tensorflow-cpu coexistence confirmed in one Python 3.10 env
# (experiments/phase4_detection/docker/Dockerfile, README.md's "kraken + TF coexistence check").
# tensorflow-cpu itself is NOT listed here - official PyPI wheels require AVX and segfault
# (exit 139) on hosts without it (found running on a Xeon W3680/Westmere, pre-dates AVX). Installed
# in api/Dockerfile instead, from a community wheel built for that case.
kraken==4.3.13
shapely
pillow
fastapi
uvicorn[standard]
pydantic
python-multipart
anthropic

# kraken's own dependency pin leaves rich unbounded; pip resolves it to rich>=15, which is
# incompatible with the pytorch_lightning version kraken pulls in at training/fine-tuning time
# (experiments/phase4_detection/README.md). Not exercised by this API (pure inference, no
# ketos/pytorch_lightning training path), but pinned anyway for a consistent, already-validated
# dependency set across the two Dockerfiles.
rich<14
